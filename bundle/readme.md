# properties

-   settings: Settings object defined in the bundle's property in the package.json (Dynamic Processor).
-   config: Settings object defined in the module.json file (Dynamic Processor).
-   meta: Each bundle exports a meta object with the following properties: (Defined in the bundle's class)

    -   name: name of the bundle
    -   Bundle: Bundle class
    -   transversal?: boolean
